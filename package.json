{
  "name": "universal-job-finder",
  "version": "1.0.0",
  "private": true,
  "description": "Upload a resume, get matching job openings with company, location, pay and platform.",
  "type": "module",
  "engines": { "node": ">=18" },
  "scripts": {
    "dev": "netlify dev",
    "deploy": "netlify deploy --prod"
  }
}
